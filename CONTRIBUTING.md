# Contributing

This is a personal project. For reference:

## Development

```bash
cd src
npm install
npm run dev
```

## Checks

Before committing, ensure:
- `npm run lint` passes
- `npm run typecheck` passes
- `npm run test` passes
